export const environment = {
    production: false,
    auth: {
        oidc: {
            // issuer: '',
            // token: '',
            // clientId: '',
            // scope: 'openid profile email',
            // requireHttps: false,
        }
    }
};
